//for in

const peaple = {
    tipe: 'Human',
    age : '100'

}

for (let chave in peaple){
    console.log(chave, pessoa['nome'])
}

const cores = ['azul', 'verde','preto']


// Este tipo de for é usado para percorrer objetos.